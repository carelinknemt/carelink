---
paths:
  - '**'
---

# General

## Commit and push after finishing work
Always finish task work by committing and pushing to GitHub (assuming the user asked for it). After all tests/type-checks pass, run pint --dirty, stage only intended files, write a concise commit message, and push to origin main.
